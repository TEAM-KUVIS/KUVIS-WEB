import { header, headerTitle } from "./Header.style";

const Header = () => {
  return (
    <header css={header}>
      <h1 css={headerTitle}>KUVIS</h1>
    </header>
  );
};

export default Header;
